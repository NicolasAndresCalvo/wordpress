# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: example-ingress
#   annotations:
#     nginx.ingress.kubernetes.io/use-regex: "true"
#   namespace: wordpress
# spec:
#   rules:
#     - host: wordpress.info
#       http:
#         paths:
#           - path: /.*
#             pathType: Prefix
#             backend:
#               service:
#                 name: wordpress
#                 port:
#                   number: 80
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
  annotations:
    # nginx.ingress.kubernetes.io/use-regex: "true"
      kubernetes.io/ingress.class: alb
      alb.ingress.kubernetes.io/subnets: subnet-0fd06287ca1408481, subnet-0dad1b9e72e21481b, subnet-04c047ae2c0d58cb2
  namespace: wordpress
spec:
  rules:
    # - host: wordpress.info
    - http:
        paths:
          - path: /.*
            pathType: Prefix
            backend:
              service:
                name: wordpress
                port:
                  number: 80